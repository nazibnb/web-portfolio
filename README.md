# calculator.py

def calcular
ret
    elip
         
    e
        i lov you
 re  
    retunvld 
fhh
if __name__ == "n_":
    a = float(input(" nmber:
    op = input("Operator (+, -, *,  ")
    b = float(inut("Second : "))
    print("Result:", calculate(a, b, op))
