# calculator.py

def calcular
ret
    elip
         
    ei love y
    
       
fhh
if __name__oat(inpu" 
    op = input"Operat+, -*, 
    b = float(inut("Secon: "))
    print("Result:", calculate(a, b, op))
