# calculator.py

def calcular
ret
    elip
         
    e
        i lov you
 re  
    retunvld 
fhh
if __name__oat(input(" 
    op = input("Operator (+, -, *, 
    b = float(inut("Second : "))
    print("Result:", calculate(a, b, op))
